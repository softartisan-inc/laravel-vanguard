# Composer Related
composer.lock
/vendor

# Frontend Assets
/node_modules

# Logs
*.log

# Caches
.phpunit.cache
.phpunit.result.cache
/build

# IDE Helper
_ide_helper.php
_ide_helper_models.php
.phpstorm.meta.php

# Editors
/.idea
/.fleet
/.vscode

# Misc
# Local PHPUnit override only. Anchored and exact, so the versioned
# phpunit.xml.dist next to it is never caught by this rule.
/phpunit.xml
phpstan.neon
testbench.yaml
/docs
/coverage
# Agent scratch: SDD workspaces, reports. Never part of the package.
/.superpowers
